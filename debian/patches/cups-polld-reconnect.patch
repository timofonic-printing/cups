--- a/scheduler/cups-polld.c
+++ b/scheduler/cups-polld.c
@@ -291,6 +291,7 @@
     fprintf(stderr, "ERROR: %s CUPS-Get-Printers failed: %s\n", prefix,
             cupsLastErrorString());
     ippDelete(response);
+    restart_polling = 1;
     return (-1);
   }
 
