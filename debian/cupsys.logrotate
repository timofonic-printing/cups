/var/log/cups/*log {
	daily
	missingok
	rotate 7
	sharedscripts
	postrotate
		[ -e /var/run/cups/cupsd.pid ] && invoke-rc.d --quiet cupsys restart > /dev/null && sleep 10
	endscript
	compress
	notifempty
}
