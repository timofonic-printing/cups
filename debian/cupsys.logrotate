/var/log/cups/*log {
	daily
	missingok
	rotate 7
	sharedscripts
	postrotate
		[ -e /var/run/cups/cupsd.pid ] && invoke-rc.d --quiet cupsys reload > /dev/null && sleep 10
	endscript
	compress
	notifempty
	create 640 root lp
}
